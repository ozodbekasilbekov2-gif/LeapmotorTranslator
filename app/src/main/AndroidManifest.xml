<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leapmotor.translator">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />



    <application
        android:name=".TranslatorApp"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LeapmotorTranslator">

        <!-- Main Activity for Settings/Control -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LeapmotorTranslator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Legacy Dictionary Activity -->
        <activity
            android:name=".DictionaryActivity"
            android:exported="false"
            android:label="Dictionary"
            android:theme="@style/Theme.LeapmotorTranslator" />

        <!-- New Dictionary Activity (with ViewModel) -->
        <activity
            android:name=".ui.dictionary.DictionaryActivity"
            android:exported="false"
            android:label="Dictionary V2"
            android:theme="@style/Theme.LeapmotorTranslator" />

        <!-- Debug History Activity -->
        <activity
            android:name=".RecognizedWordsActivity"
            android:exported="false"
            android:label="Debug History"
            android:theme="@style/Theme.LeapmotorTranslator" />

        <!-- Translation Overlay Accessibility Service -->
        <service
            android:name=".TranslationService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="@string/service_name">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Boot Receiver to start service on boot -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
